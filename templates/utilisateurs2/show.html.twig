{% extends 'base.html.twig' %}

{% block title %}Utilisateurs{% endblock %}

{% block body %}
    <h1>Utilisateurs</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ utilisateur.id }}</td>
            </tr>
            <tr>
                <th>Nom</th>
                <td>{{ utilisateur.Nom }}</td>
            </tr>
            <tr>
                <th>Prenom</th>
                <td>{{ utilisateur.Prenom }}</td>
            </tr>
            <tr>
                <th>Age</th>
                <td>{{ utilisateur.Age }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_utilisateurs2_index') }}">back to list</a>

    <a href="{{ path('app_utilisateurs2_edit', {'id': utilisateur.id}) }}">edit</a>

    {{ include('utilisateurs2/_delete_form.html.twig') }}
{% endblock %}
